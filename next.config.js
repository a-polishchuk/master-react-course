/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/mastering-react-landing',
    images: {
        unoptimized: true,
    },
};

module.exports = nextConfig; 